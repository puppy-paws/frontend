import { style } from "@vanilla-extract/css";

export const flexColumnContentsCenter = style({
  display: "flex",
  justifyContent: "center",
  flexDirection: "column",
});

export const flexRowContentsCenter = style({
  display: "flex",
  justifyContent: "center",
  flexDirection: "row",
});
